# bare-lm

A minimal autograd tensor library in C for building language models.

## Build & Run

```bash
make run FILE=<filename>
```

## Features (WIP, heavily!)

- Tensor operations: add, sub, mul, div, neg, pow, exp, log, sum, mean, dot
- Automatic differentiation via backward pass
